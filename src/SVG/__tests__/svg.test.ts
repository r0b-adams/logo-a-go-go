describe("SVG", () => {
  it("has dimensions width 300px and height 200px", () => {});
  it("sets and gets text element template", () => {});
  it("sets and gets shape element template shape", () => {});
  it("throws exception if render called with no text", () => {});
  it("throws exception if render called with no shape", () => {});
  it("renders an svg template with given parameters", () => {});
});
